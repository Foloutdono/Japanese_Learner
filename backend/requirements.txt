pydantic>=2.0
fastapi>=0.100
uvicorn[standard]
psycopg2-binary
PyJWT
cryptography
httpx
aiofiles
python-multipart
pykakasi
requests>=2.31.0
fugashi
unidic-lite
pytest>=9.0
# study/exam_tts.py's listening-audio synthesis -- a thin async client
# for Microsoft Edge's free consumer "Read Aloud" service (the same
# free voices freetts.org's own /api/tts widget proxies), not a paid
# API and not a locally-hosted model. Free, no API key, no vendor
# account.
edge-tts>=7.0.0
# main.py loads backend/.env through this. It arrives transitively
# with uvicorn[standard] today, which is not something to rely on.
python-dotenv
# study/captions.py's best-effort YouTube caption fetch. Scrapes
# YouTube's own timedtext endpoints rather than calling an official API
# (YouTube publishes none for this) -- expected to fail with IpBlocked/
# RequestBlocked from a datacenter IP, which is exactly what this
# backend deploys on (Render). See docs/adr/0003-source-agnostic-
# caption-pipeline.md: the upload path exists precisely because of this,
# not as a fallback bolted on afterwards.
youtube-transcript-api>=1.2.0
