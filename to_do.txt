general :
- divide the furiganas to appear above the kanji they belong to


dictionnary :
- vary used in these word to capture more readings.
- preselect kana to fill the space better
- remove the border top of the stroke order for the kanas detail card
- add padding at the end of the kanas section on mobile to prevent the xp of hidding content
- reduce the section header margin bottom on mobile



indice_1 = mcq choices (activatable by the user if he has difficulty on a card)
indice_2 = japanese sentences examples with no traduction until reveal (activatable by the user if he has difficulty on a card)
indice_3 = furiganas (activatable by the user if he has difficulty on a card)
(f->b) = front to back
(b->f) = back to front
fill in = japanese sentence with missing parts (no taduction) -> find grammar rule (works like a flashcard sentence -> rule)
fast review = browse the flashcards (invertible from (f->b) to (b->f))
write romaji = kana -> write the romaji
write kana = romaji -> write kana (drawing canvas)
write kanji = meaning -> write kanji (drawing canvas)
readings = kanji -> write the readings in romaji or kanas with the keyboard
radical recognition = kanji -> radical (flashcard)
reading = vocab -> reading in kanas (flashcard + only vocab with kanji in it)

kana -> 1. flashcard (f->b) + indice_1 2. flashcard (b->f) + indice_1 3. write romaji 4. write kana 5. fast review
kanji -> 1. flashcard (f->b) + indice_1 2. flashcard (b->f) + indice_1 3. write kanji 4. readings 5. radical recognition 6. fast review
vocab -> 1. flashcard (f->b) + indice_1 or indice_3  2. flashcard (b->f) + indice_1 or indice_3  3. reading 4. fast review
grammar -> 1. flashcard (f->b) + indice_1 or indice_2 2. flashcard (b->f) + indice_1 or indice_2 3. fill in + indice_1 4. fast review

personal flashcard -> 1. standart 2. kanji structure 3. vocab structure 4. grammar structure
standart = front + back (no hint and notes anymore)
kanji structure = create your own kanji card must have kanji, meaning, readings, radical number (selection menu for add a card)
vocab structure = create your own vocab card must have japanese word, meaning, optional reading
grammar structure = create your own grammar card must have japanese rule, meaning, optional sentences (must contain the rules if not drop the card in fill in)

no mixed decks anymore, if the personal card is a kanji, vocab, grammar one -> access to their game modes

standart -> 1. flashcard (f->b) + indice_1 2. flashcard (b->f) + indice_1 3. fast review